// Generated by Ignite ignite.com/cli

import { StdFee } from "@cosmjs/launchpad";
import { SigningStargateClient, DeliverTxResponse } from "@cosmjs/stargate";
import { EncodeObject, GeneratedType, OfflineSigner, Registry } from "@cosmjs/proto-signing";
import { msgTypes } from './registry';
import { IgniteClient } from "../client"
import { MissingWalletError } from "../helpers"
import { Api } from "./rest";
import { MsgCreateBet } from "./types/lottery/lottery/tx";
import { MsgUpdateSystemInfo } from "./types/lottery/lottery/tx";


export { MsgCreateBet, MsgUpdateSystemInfo };

type sendMsgCreateBetParams = {
  value: MsgCreateBet,
  fee?: StdFee,
  memo?: string
};

type sendMsgUpdateSystemInfoParams = {
  value: MsgUpdateSystemInfo,
  fee?: StdFee,
  memo?: string
};


type msgCreateBetParams = {
  value: MsgCreateBet,
};

type msgUpdateSystemInfoParams = {
  value: MsgUpdateSystemInfo,
};


export const registry = new Registry(msgTypes);

const defaultFee = {
  amount: [],
  gas: "200000",
};

interface TxClientOptions {
  addr: string
	prefix: string
	signer?: OfflineSigner
}

export const txClient = ({ signer, prefix, addr }: TxClientOptions = { addr: "http://localhost:26657", prefix: "cosmos" }) => {

  return {
		
		async sendMsgCreateBet({ value, fee, memo }: sendMsgCreateBetParams): Promise<DeliverTxResponse> {
			if (!signer) {
					throw new Error('TxClient:sendMsgCreateBet: Unable to sign Tx. Signer is not present.')
			}
			try {			
				const { address } = (await signer.getAccounts())[0]; 
				const signingClient = await SigningStargateClient.connectWithSigner(addr,signer,{registry, prefix});
				let msg = this.msgCreateBet({ value: MsgCreateBet.fromPartial(value) })
				return await signingClient.signAndBroadcast(address, [msg], fee ? fee : defaultFee, memo)
			} catch (e: any) {
				throw new Error('TxClient:sendMsgCreateBet: Could not broadcast Tx: '+ e.message)
			}
		},
		
		async sendMsgUpdateSystemInfo({ value, fee, memo }: sendMsgUpdateSystemInfoParams): Promise<DeliverTxResponse> {
			if (!signer) {
					throw new Error('TxClient:sendMsgUpdateSystemInfo: Unable to sign Tx. Signer is not present.')
			}
			try {			
				const { address } = (await signer.getAccounts())[0]; 
				const signingClient = await SigningStargateClient.connectWithSigner(addr,signer,{registry, prefix});
				let msg = this.msgUpdateSystemInfo({ value: MsgUpdateSystemInfo.fromPartial(value) })
				return await signingClient.signAndBroadcast(address, [msg], fee ? fee : defaultFee, memo)
			} catch (e: any) {
				throw new Error('TxClient:sendMsgUpdateSystemInfo: Could not broadcast Tx: '+ e.message)
			}
		},
		
		
		msgCreateBet({ value }: msgCreateBetParams): EncodeObject {
			try {
				return { typeUrl: "/lottery.lottery.MsgCreateBet", value: MsgCreateBet.fromPartial( value ) }  
			} catch (e: any) {
				throw new Error('TxClient:MsgCreateBet: Could not create message: ' + e.message)
			}
		},
		
		msgUpdateSystemInfo({ value }: msgUpdateSystemInfoParams): EncodeObject {
			try {
				return { typeUrl: "/lottery.lottery.MsgUpdateSystemInfo", value: MsgUpdateSystemInfo.fromPartial( value ) }  
			} catch (e: any) {
				throw new Error('TxClient:MsgUpdateSystemInfo: Could not create message: ' + e.message)
			}
		},
		
	}
};

interface QueryClientOptions {
  addr: string
}

export const queryClient = ({ addr: addr }: QueryClientOptions = { addr: "http://localhost:1317" }) => {
  return new Api({ baseURL: addr });
};

class SDKModule {
	public query: ReturnType<typeof queryClient>;
	public tx: ReturnType<typeof txClient>;
	
	public registry: Array<[string, GeneratedType]> = [];

	constructor(client: IgniteClient) {		
	
		this.query = queryClient({ addr: client.env.apiURL });		
		this.updateTX(client);
		client.on('signer-changed',(signer) => {			
		 this.updateTX(client);
		})
	}
	updateTX(client: IgniteClient) {
    const methods = txClient({
        signer: client.signer,
        addr: client.env.rpcURL,
        prefix: client.env.prefix ?? "cosmos",
    })
	
    this.tx = methods;
    for (let m in methods) {
        this.tx[m] = methods[m].bind(this.tx);
    }
	}
};

const Module = (test: IgniteClient) => {
	return {
		module: {
			LotteryLottery: new SDKModule(test)
		},
		registry: msgTypes
  }
}
export default Module;